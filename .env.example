OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini

# Canvas defaults (optional if you pass x-canvas-base-url and x-canvas-token per request)
CANVAS_BASE_URL=
CANVAS_API_TOKEN=

# Auth encryption key for login session cookie (32-byte plain, 64-char hex, or base64 for 32 bytes)
AUTH_ENCRYPTION_KEY=

# Persistent auth DB (required for multi-device account login)
# Use your Neon/Vercel Postgres pooled connection string.
POSTGRES_URL=
